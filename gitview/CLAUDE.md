This project depends on two other projects:

- git-changes: ~/tools/git-changes
- tui : ~/tools/pager/tui

Don't try to workaround bugs or shortcomings of those libraries here, instead
fix them directly in the library and report to the user.

