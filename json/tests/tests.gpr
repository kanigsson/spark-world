with "../json.gpr";

project Tests is
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("test_json.adb", "test_walk.adb");

   --  All checks on: the harness treats any propagated exception as a
   --  test failure.
   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2012", "-gnata", "-g", "-O1");
   end Compiler;
end Tests;
